<h1><%= @post.title %></h1>
<p>Posted by: <%= @post.user.email %> <%= time_ago_in_words(@post.created_at) %> ago </p>
<p><%= @post.content %></p>
<h3>Comments:</h3>
<%= render @post.comments %>
<h3>Reply to Post:</h3>
<%= render 'comments/form' %>

<%= link_to "Back", posts_path%>
<% if user_signed_in? %>
<% if @post.user_id == current_user.id %>
<%= link_to "Edit", edit_post_path(@post) %>
<%= link_to "Delete", post_path(@post), method: :delete,
    data: {
         :'sweet-alert-confirm' => "Are you sure you want to delete this post?",
         :'confirm-button-text' => 'Delete',
         :'cancel-button-text' => 'Cancel',
         :'confirm-button-color' => '#D9534F',
         :'sweet-alert-type' => 'error',
         text: 'This cannot be undone.'
         } %>

<% end %>
<% end %>
<!-- make it username and add link to go to that users profile page  -->
